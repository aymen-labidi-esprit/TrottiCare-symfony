import './bootstrap.js'; // If generated by --webapp, usually imports bootstrap JS
/*
 * Welcome to your app's main JavaScript file!
 *
 * We recommend including the built version of this JavaScript file
 * (and its CSS file) in your base layout (base.html.twig).
 */

// Import Bootstrap CSS
import 'bootstrap/dist/css/bootstrap.min.css';
// Import Bootstrap Icons CSS
import 'bootstrap-icons/font/bootstrap-icons.css';
require('@fortawesome/fontawesome-free/css/all.min.css');
require('@fortawesome/fontawesome-free/js/all.js');

// Import your custom CSS
import './styles/app.css';

// Import Bootstrap's JS if you need dropdowns, modals, etc.
// Note: --webapp might already include this via ./bootstrap.js
// import 'bootstrap';


// Simple delete confirmation JS
document.addEventListener('DOMContentLoaded', () => {
    const deleteForms = document.querySelectorAll('form.delete-form');
    deleteForms.forEach(form => {
        form.addEventListener('submit', (event) => {
            const message = form.getAttribute('data-confirm-message') || 'Êtes-vous sûr de vouloir supprimer cet élément ?';
            if (!confirm(message)) {
                event.preventDefault();
            }
        });
    });
});

console.log('TrottiCare assets loaded!');